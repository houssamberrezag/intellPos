import { FormatLongDatePipe } from './format-long-date.pipe';

describe('FormatLongDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLongDatePipe();
    expect(pipe).toBeTruthy();
  });
});
