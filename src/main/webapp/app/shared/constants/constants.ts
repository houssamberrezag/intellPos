import { IPerson } from 'app/entities/person/person.model';

export const DEFAULT_CLIENT: IPerson = {
  id: 0,
  firstName: 'Client par défaut',
  lastName: '',
};

export const DEFAULT_SUPPLIER: IPerson = {
  id: 0,
  firstName: 'Fournisseur par défaut',
  lastName: '',
};
